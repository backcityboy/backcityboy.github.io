=== Códigos para serem usados diariamente durante a realização de atividades (em ordem sequencial): ===

1. cmdkey /list | findstr /I "git:"
2. cmdkey /delete:"{O que tiver escrito após 'target=}"
3. git config user.name "backcityboy"
4. git config user.email "lopesyandiego@gmail.com"
5. git init
6. git remote add origin https://github.com/backcityboy/backcityboy.github.io.git
7. git add .
8. git commit -m "comentário qualquer"
9. git push origin main (só pá dá merda XD)
10.git pull origin main --allow-unrelated-histories
11.git push origin main (agora sim é pá dá baum)


(medida de segurança: git branch -m main)
